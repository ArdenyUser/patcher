fil = input("Select a .PAT file: ")
file1 = open(fil, 'r')
Lines = file1.readlines()
 
count = 1
for line in Lines:
    if count == 1:
      myfile = open(fil, 'a')
    if count == 2:
      linenum = 
    if count
      myfile.writelines('Mage')[1]
      myfile.close()
    count += 1
